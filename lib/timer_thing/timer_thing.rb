# This class is sort of a top-level catchall, but for a start is responsible for
# knowing how to convert time in seconds into text formats.
module TimerThing
  class TimerThing
    # Returns seconds in minutes an seconds, in a format like "15m3s" or "0m0s"
    def sec_to_minsec(seconds)
      ""
    end
  end
end
